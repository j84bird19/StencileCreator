# StencilForge v3

Flat GitHub Pages build.

Added: crop selection, magic selection, undo last, cleaner stencil UI, smoother stencil previews, SVG layer export, layer colors, registration marks, save/load settings.

Upload the extracted files to the root of your GitHub repo. Do not upload the ZIP itself.
